{% extends 'base.html.twig' %}

{% block title %}EstadoTarea index{% endblock %}

{% block body %}
    <h1>EstadoTarea index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for estado_tarea in estado_tareas %}
            <tr>
                <td>{{ estado_tarea.id }}</td>
                <td>{{ estado_tarea.nombre }}</td>
                <td>{{ estado_tarea.descripcion }}</td>
                <td>
                    <a href="{{ path('app_estado_tarea_show', {'id': estado_tarea.id}) }}">show</a>
                    <a href="{{ path('app_estado_tarea_edit', {'id': estado_tarea.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_estado_tarea_new') }}">Create new</a>
{% endblock %}
